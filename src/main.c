#include <stdio.h>
#include <stdlib.h>

#include "lib.h"

/*
 * Implement a basic array.
 */

int main() {
  // print_points();

  printf("sizeof(Element *) = %d\n", sizeof(Element *));
  
  return 0;
}
